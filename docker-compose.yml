services:
  # PostgreSQL Database
  postgres:
    image: postgres:15-alpine
    container_name: destack_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-destack_db}
      POSTGRES_USER: ${POSTGRES_USER:-destack_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?defina POSTGRES_PASSWORD no .env}
      POSTGRES_INITDB_ARGS: "--encoding=UTF8"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backups:/backups
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-destack_user} -d ${POSTGRES_DB:-destack_db}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - destack_network

  # Redis Cache
  redis:
    image: redis:7-alpine
    container_name: destack_redis
    restart: unless-stopped
    command: redis-server --appendonly yes --requirepass ${REDIS_PASSWORD:?defina REDIS_PASSWORD no .env}
    environment:
      REDIS_PASSWORD: ${REDIS_PASSWORD:?defina REDIS_PASSWORD no .env}
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD-SHELL", "redis-cli -a \"$REDIS_PASSWORD\" ping | grep PONG"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - destack_network

  # Django Application (Backend API)
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: destack_web
    env_file:
      - .env
    restart: unless-stopped
    environment:
      - DEBUG=False
      - DJANGO_SECRET_KEY=${DJANGO_SECRET_KEY:?defina DJANGO_SECRET_KEY no .env}
      - ALLOWED_HOSTS=${ALLOWED_HOSTS:?defina ALLOWED_HOSTS no .env}
      - CSRF_TRUSTED_ORIGINS=${CSRF_TRUSTED_ORIGINS:?defina CSRF_TRUSTED_ORIGINS no .env}
      - CORS_ALLOWED_ORIGINS=${CORS_ALLOWED_ORIGINS:?defina CORS_ALLOWED_ORIGINS no .env}
      - DATABASE_URL=postgres://${POSTGRES_USER:-destack_user}:${POSTGRES_PASSWORD:?defina POSTGRES_PASSWORD no .env}@postgres:5432/${POSTGRES_DB:-destack_db}
      - REDIS_PASSWORD=${REDIS_PASSWORD:?defina REDIS_PASSWORD no .env}
      - REDIS_URL=redis://:${REDIS_PASSWORD:?defina REDIS_PASSWORD no .env}@redis:6379/0
      - REDIS_CACHE_URL=redis://:${REDIS_PASSWORD:?defina REDIS_PASSWORD no .env}@redis:6379/15
      - CELERY_BROKER_URL=redis://:${REDIS_PASSWORD:?defina REDIS_PASSWORD no .env}@redis:6379/0
      - CELERY_RESULT_BACKEND=redis://:${REDIS_PASSWORD:?defina REDIS_PASSWORD no .env}@redis:6379/0
    volumes:
      - static_volume:/app/staticfiles
      - media_volume:/app/media
      - ./logs:/app/logs
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - destack_network

  # Frontend React (Nginx serving built files)
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.prod
    container_name: destack_frontend
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/ssl:/etc/nginx/ssl:ro
    depends_on:
      - web
    networks:
      - destack_network

  # EGS XML Collector - Robot para coleta automatica de XMLs
  scraper:
    # Google Chrome só tem build amd64; em hosts arm64 (Apple Silicon) roda via emulação.
    platform: linux/amd64
    build:
      context: ./scraper
      dockerfile: Dockerfile
    container_name: destack_scraper
    restart: unless-stopped
    # docker-init (tini) recolhe processos Chrome orfaos ao fim de cada job.
    init: true
    pids_limit: 512
    environment:
      # EGS Sistemas
      - EGS_BASE_URL=https://app.egssistemas.com.br
      - EGS_LOGIN_URL=https://app.egssistemas.com.br/login
      - EGS_USERNAME=${EGS_USERNAME:-DESTACK}
      - EGS_PASSWORD=${EGS_PASSWORD:?defina EGS_PASSWORD no .env}
      - EGS_ACCESS_KEY=${EGS_ACCESS_KEY:-57226}
      # Destack API (produção: destack_app na rede apps_backend)
      - DESTACK_API_URL=${DESTACK_API_URL:-http://destack:8000/api}
      - DESTACK_USERNAME=${DESTACK_USERNAME:-admin}
      - DESTACK_PASSWORD=${DESTACK_PASSWORD:?defina DESTACK_PASSWORD no .env}
      # Redis (utilizado pelo scraper para cache/fila, se aplicável)
      - REDIS_PASSWORD=${REDIS_PASSWORD:?defina REDIS_PASSWORD no .env}
      - REDIS_URL=redis://:${REDIS_PASSWORD:?defina REDIS_PASSWORD no .env}@redis:6379/0
      # Collector
      - COLLECT_INTERVAL_MINUTES=60
      - DAYS_LOOKBACK=7
      - LOG_LEVEL=INFO
      - SELENIUM_HEADLESS=true
    volumes:
      - scraper_data:/app/data
      - scraper_logs:/app/logs
    depends_on:
      - web
    networks:
      - destack_network
      - apps_backend

  # Celery Worker (para tarefas assincronas)
  celery_worker:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: destack_celery_worker
    restart: unless-stopped
    init: true
    pids_limit: 256
    command: celery -A core worker -l info
    environment:
      - DEBUG=False
      - DJANGO_SECRET_KEY=${DJANGO_SECRET_KEY:?defina DJANGO_SECRET_KEY no .env}
      - DATABASE_URL=postgres://${POSTGRES_USER:-destack_user}:${POSTGRES_PASSWORD:?defina POSTGRES_PASSWORD no .env}@postgres:5432/${POSTGRES_DB:-destack_db}
      - REDIS_PASSWORD=${REDIS_PASSWORD:?defina REDIS_PASSWORD no .env}
      - REDIS_URL=redis://:${REDIS_PASSWORD:?defina REDIS_PASSWORD no .env}@redis:6379/0
      - REDIS_CACHE_URL=redis://:${REDIS_PASSWORD:?defina REDIS_PASSWORD no .env}@redis:6379/15
      - CELERY_BROKER_URL=redis://:${REDIS_PASSWORD:?defina REDIS_PASSWORD no .env}@redis:6379/0
      - CELERY_RESULT_BACKEND=redis://:${REDIS_PASSWORD:?defina REDIS_PASSWORD no .env}@redis:6379/0
    volumes:
      - ./logs:/app/logs
    depends_on:
      - redis
      - postgres
    networks:
      - destack_network

  # Celery Beat (agendador de tarefas)
  celery_beat:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: destack_celery_beat
    restart: unless-stopped
    init: true
    pids_limit: 64
    command: celery -A core beat -l info
    environment:
      - DEBUG=False
      - DJANGO_SECRET_KEY=${DJANGO_SECRET_KEY:?defina DJANGO_SECRET_KEY no .env}
      - DATABASE_URL=postgres://${POSTGRES_USER:-destack_user}:${POSTGRES_PASSWORD:?defina POSTGRES_PASSWORD no .env}@postgres:5432/${POSTGRES_DB:-destack_db}
      - REDIS_PASSWORD=${REDIS_PASSWORD:?defina REDIS_PASSWORD no .env}
      - REDIS_URL=redis://:${REDIS_PASSWORD:?defina REDIS_PASSWORD no .env}@redis:6379/0
      - REDIS_CACHE_URL=redis://:${REDIS_PASSWORD:?defina REDIS_PASSWORD no .env}@redis:6379/15
      - CELERY_BROKER_URL=redis://:${REDIS_PASSWORD:?defina REDIS_PASSWORD no .env}@redis:6379/0
      - CELERY_RESULT_BACKEND=redis://:${REDIS_PASSWORD:?defina REDIS_PASSWORD no .env}@redis:6379/0
    volumes:
      - ./logs:/app/logs
    depends_on:
      - redis
      - postgres
    networks:
      - destack_network

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local
  static_volume:
    driver: local
  media_volume:
    driver: local
  scraper_data:
    driver: local
  scraper_logs:
    driver: local

networks:
  destack_network:
    driver: bridge
  apps_backend:
    external: true
